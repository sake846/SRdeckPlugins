# SRdeckPlugin.Analog

安定プラグインIDは `analog`。AM、FM、SSBの音声復調を提供します。

## プロファイル

- AM（振幅変調）
- FM（周波数変調）
- SSB（単側波帯）

## 機能

プラグイン独自の音声ワークスペースと設定画面で、プロファイル選択、同調、スケルチ、AGC、
音声フィルター、直前3秒のIQプリトリガー録音を扱えます。ヘッドレス処理にも対応します。

## ビルド

互換するSRdeckプラットフォームパッケージを用意し、`SRdeckPlugins.sln`のRelease構成をビルドしてください。
